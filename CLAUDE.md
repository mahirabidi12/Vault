# PkgGuard

Before doing anything, read in this order:
1. `PROGRESS.md`: what's built, how to run it, open decisions, conventions
2. `README.md`: full project plan, architecture, decisions and gotchas
3. `FUTURE_SCOPE.md`: postponed ideas (MCP/skill scanning, dynamic sandbox)

Key rules: the user commits manually (give commands, never commit) · explain finished steps briefly in easy words · keep `cd analyzer && uv run pytest -q` passing · never run package code · never commit secrets · update `PROGRESS.md` after every step.
