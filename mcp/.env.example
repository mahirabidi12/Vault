# Base URL of the PkgGuard API (no trailing slash).
# Cloud, once deployed:  https://<api-id>.execute-api.<region>.amazonaws.com
# Local dev (no AWS):    http://127.0.0.1:8787   (start with `uv run pkgguard-dev-api` in analyzer/)
PKGGUARD_API_URL=http://127.0.0.1:8787

# Optional: website base URL, used to build a human-readable report link.
# Falls back to the raw JSON report from the API if unset.
PKGGUARD_WEB_URL=

# Optional: how long to poll a scan that hasn't finished yet, before telling
# the agent to check back later instead of installing.
PKGGUARD_POLL_INTERVAL_MS=1500
PKGGUARD_MAX_WAIT_MS=45000
